"use client"; import { IconCheck } from "@tabler/icons-react"; import { type ThemeId, THEMES, getThemeLabel,
} from "@/features/theme/theme.constants";
import { cn } from "@/lib/utils"; interface ThemeGridProps { currentTheme: string; onSelect: (id: ThemeId) => void;
} export function ThemeGrid({ currentTheme, onSelect }: ThemeGridProps) { return ( <div className="px-4 pb-5"> <div className="mb-3 flex items-center justify-between"> <p className="text-xs text-muted-foreground">Select a curated palette.</p> <p className="text-xs font-medium text-foreground"> Current: {getThemeLabel(currentTheme)} </p> </div> <div className="grid grid-cols-2 gap-3 sm:grid-cols-3" role="radiogroup" aria-label="Theme options"> {THEMES.map((theme) => { const isSelected = theme.id === currentTheme; return ( <button key={theme.id} type="button" role="radio" aria-checked={isSelected} onClick={() => onSelect(theme.id)} className={cn( "relative rounded-xl border border-border bg-card p-3 text-left transition-all hover:border-primary/60 hover:bg-accent/40 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring/60", isSelected && "border-primary bg-accent/60 ring-2 ring-primary/30", )} > <div className="mb-2 flex items-center justify-between"> <span className="text-sm font-medium text-card-foreground">{theme.label}</span> {isSelected ? ( <span className="rounded-full bg-primary p-1 text-primary-foreground"> <IconCheck className="size-3" stroke={3} /> </span> ) : null} </div> <div className="flex items-center gap-2"> <span className="size-4 rounded-full border border-border/60" style={{ backgroundColor: theme.preview.primary }} /> <span className="size-4 rounded-full border border-border/60" style={{ backgroundColor: theme.preview.background }} /> <span className="size-4 rounded-full border border-border/60" style={{ backgroundColor: theme.preview.accent }} /> </div> </button> ); })} </div> </div> );
}
