"use client"; import { useState, useEffect } from "react";
import { Button } from "@/components/ui/button"; export function BetaBanner() { const [isVisible, setIsVisible] = useState(false); useEffect(() => { const isHidden = localStorage.getItem("hide_beta_banner"); if (!isHidden) { setIsVisible(true); } }, []); if (!isVisible) return null; const handleDismiss = () => { localStorage.setItem("hide_beta_banner", "true"); setIsVisible(false); }; return ( <div className="mb-2 flex items-center justify-between gap-4 rounded-xl border border-indigo-200 bg-indigo-50 px-4 py-3 text-sm text-indigo-900 shadow-sm dark:border-indigo-900/60 dark:bg-indigo-950/30 dark:text-indigo-200"> <div className="flex-1"> <span className="font-semibold">Notice:</span> Studytrix is currently in beta and is actively being developed. You might experience occasional bugs or changes. </div> <Button variant="ghost" size="sm" onClick={handleDismiss} className="h-8 shrink-0 text-indigo-700 hover:bg-indigo-100/80 hover:text-indigo-900 dark:text-indigo-300 dark:hover:bg-indigo-900/50 dark:hover:text-indigo-100" > Don&apos;t Show Again </Button> </div> );
}
